# how to checkout to a different github branch in yaml using azure devops

resources:

  repositories:

  - repository: br_workspace_publish
    type: github
    endpoint: clintgrove
    name: clintgrove/azure-synapse-bicep-v2
    ref: workspace_publish
